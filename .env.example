# ============ Gemini 原生 API 配置示例 ============
# OPENAI_BASE_URL=https://generativelanguage.googleapis.com/v1beta
# GEMINI_API_KEY=your_gemini_api_key
# OPENAI_MODEL=gemini-2.5-flash-image
# OPENAI_IMAGE_MODE=gemini

# ============ OpenAI 原生 API 配置示例 ============
# OPENAI_BASE_URL=https://api.openai.com
# OPENAI_API_KEY=your_openai_api_key
# OPENAI_MODEL=dall-e-3
# OPENAI_IMAGE_MODE=openai

# ============ 本地代理配置示例 ============
OPENAI_BASE_URL=http://127.0.0.1:8317
# OPENAI_API_KEY=your_key_if_needed
OPENAI_MODEL=gemini-3-pro-image-preview

# API 模式选择:
# - gemini: Gemini 原生 API (generateContent 端点)
# - openai: OpenAI 原生 API (images/generations 端点)
# - chat: OpenAI chat/completions 兼容格式 (用于代理)
# - auto: 自动检测
OPENAI_IMAGE_MODE=chat

# 可选配置
# OPENAI_IMAGE_SIZE=1024x1024  # 图片尺寸
# OPENAI_IMAGE_RETURN=path     # path|image（path：保存后返回路径；image：返回 base64）
# OPENAI_IMAGE_OUT_DIR=debug-output  # 保存目录
# OPENAI_DEBUG=1               # 启用调试日志
OPENAI_TIMEOUT_MS=120000
